# Minimal requirements for building MDI AutoLogin
# These are the only packages actually needed by the application

# Core dependencies
requests>=2.32.0
urllib3>=2.5.0
keyring>=25.6.0
pystray>=0.19.5
pillow>=11.3.0
six>=1.17.0  # Required by pystray

# Build tool
pyinstaller>=6.16.0
pyinstaller-hooks-contrib>=2025.9

# Windows-specific (only needed on Windows)
pywin32-ctypes>=0.2.3; sys_platform == "win32"

